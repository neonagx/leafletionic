import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'leafletIonic',
  appName: 'leafletIonic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
